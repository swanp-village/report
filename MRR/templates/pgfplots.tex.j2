\documentclass[tikz]{standalone}
\usepackage{pgfplots}
\tikzset{
  /pgf/number format/.cd,
  1000 sep={},
}
\pgfplotsset{compat=1.17}
\begin{document}
\begin{tikzpicture}[thick]
\begin{axis}[
  width=85mm,
  height=65mm,
  xlabel={Wavelength (nm)},
  ylabel={Drop Port Power (dB)},
  xlabel near ticks,
  ylabel near ticks,
  ymin=-60,
  ymax=0,
  ytick={
    -60,
    -50,
    -40,
    -30,
    -20,
    -10,
    0
  },
  legend entries={{legends}},
  legend pos=south east,
  cycle list name=color,
]
  {% for tsvname in tsvnames %}
  \addplot+[no markers] table[x expr={\thisrowno{0} * 1e9}] {{tsvname}};
  {% endfor %}
\end{axis}
\end{tikzpicture}
\end{document}
